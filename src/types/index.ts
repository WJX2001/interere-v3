export interface MenuItemsTypes {
  title: string;
  url: string;
  cName: string;
}

export interface CoinListTypes {
  name: string;
  symbol: string;
  address: string;
  logoURI: string;
}

export interface TokenInfoTypes {
  name: string;
  symbol: string;
  address: string;
  logoURI: string;
}
